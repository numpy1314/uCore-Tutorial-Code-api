# uCore 第 3 章 API 实验报告

## 版本与环境

- 学号 / 姓名：
- 学生分支、起始 commit、实现 commit：
- 参考分支 `ch3-api-impl`、阅读时的完整 commit：
- 测试仓库 commit：
- 工具链、QEMU、GDB 版本与运行命令：
- 允许修改的代码文件：见 `lab.json`（只修改列出的函数体）。

## 1. 静态分析：任务与参考源码的对应关系

每个函数至少列出一条引用，格式为 `commit / 文件路径 / 函数名 / L起始-L结束`；也可附固定 commit 的 GitHub permalink。说明调用者、输入、输出、状态或资源约束，不只粘贴代码。

| 函数 | 任务要求 | 参考源码引用 | 如何满足要求 |
| --- | --- | --- | --- |
| `proc_init` | 待填写 | 待填写 | 待填写 |
| `scheduler` | 待填写 | 待填写 | 待填写 |
| `yield` | 待填写 | 待填写 | 待填写 |
| `exit` | 待填写 | 待填写 | 待填写 |

## 2. GDB 动态跟踪

- 使用参考实现 commit：
- 断点、触发输入、执行命令：
- 调用栈与关键变量执行前 / 后的真实值：
- 日志路径（建议 `reports/gdb.log`）：
- 静态推断与动态观察是否一致，为什么：

## 3. 独立实现与对比

逐个函数描述自己的算法；完成后与参考代码对比。对每项差异说明设计理由、语义是否等价及验证证据。相同实现也要说明其约束来源，不能只写“与参考一致”。

## 4. 主要问题与解决思路

| 问题和现象 | 原因分析 | 解决方法 | 验证结果 |
| --- | --- | --- | --- |
| 待填写 | 待填写 | 待填写 | 待填写 |

## 5. 验收与修改范围

- 实际运行的命令及退出状态：
- 完整运行日志路径（建议 `reports/run.log`）：
- 预期输出与实际输出：
- `git diff <学生起始commit> --stat`：
- 代码是否只修改允许的函数体：
- 未覆盖的情况与已知限制：

## 6. 辅助工具使用说明

如使用 AI 或其他辅助工具，说明用途、关键建议、你如何验证及最终自行作出的判断；按课程要求保留 `.ai/` 中的过程记录。
